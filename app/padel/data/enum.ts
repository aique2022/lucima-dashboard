export enum TicketStatus {
  Open = "open",
  InProgress = "in-progress",
  Closed = "closed",
}

export enum TicketPriority {
  Low = "low",
  Medium = "medium",
  High = "high",
}

export enum TransStatus {
  Pending = "pending",
  Hold = "hold",
  Completed = "completed",
}
